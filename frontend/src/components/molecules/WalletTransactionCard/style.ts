import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const StyledContainer = styled(Box)({
  height: '46px',
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'center',
  width: '100%'
});
export const StyledDateContainer = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  marginRight: '12px'
});
export const StyledInnerContainer = styled(Box)({
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'space-between',
  width: '100%'
});
export const StyledMainCoinContainer = styled(Box)({
  marginLeft: '12px'
});

export const StyledCoinContainer = styled(Box)({
  display: 'flex',
  flexDirection: 'column'
});

export const StyledDetailsContainer = styled(Box)({
  display: 'flex',
  justifyContent: 'space-between'
});
export const StyledTypoContainer = styled(Box)({
  marginRight: '10px'
});

export const StyledValueContainer = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'flex-end'
});
